# Android setup

Run `flutter create . --platforms=android` with the latest stable Flutter SDK if native Android scaffolding is not present, then add `android/app/google-services.json` from Firebase. The Dart application code and dependencies are already prepared for Android-first delivery.
